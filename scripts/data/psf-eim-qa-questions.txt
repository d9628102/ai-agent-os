# End-to-end RAG QA test set — one question per line, '#' starts a comment.
# Run with:
#   python3 scripts/rag_answer.py --queries-file scripts/data/psf-eim-qa-questions.txt
#
# The first five are the same questions used for the retrieval-only test in
# psf-eim-test-questions.txt, so answer quality can be compared against the
# retrieval scores recorded there.
PSF EIM 的五大產品矩陣是什麼？
Decision Matrix 的核心模組有哪些？
PSF EIM 跟一般 ERP/CRM 有什麼不同？
PSF EIM 裡負責客戶服務的 Agent 是誰？
企業智慧架構總共分成幾層？
# Negative control: the source document says nothing about pricing. A grounded
# system must answer 「文件中未提及」 here rather than inventing a number.
PSF EIM 的定價是多少？
